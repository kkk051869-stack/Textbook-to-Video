## comparison 组件参考
- 用左右两栏或上下两块对比面板呈现对象，中央可放 VS 徽章，底部用一句总结承接教学结论。
- 每侧包含标题、简短描述、关键数据或进度条；颜色使用 `var(--primary)`、`var(--accent)` 等主题变量。
- 面板保持等高和足够间距，使用 flex/grid 与 gap 控制，避免文字、徽章和进度条互相重叠。数据标签靠近对应面板，不要遮挡 VS 区域。
