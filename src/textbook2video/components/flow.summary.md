## process 组件参考
- 用横向或纵向 flex 展示 2-6 个步骤，每步包含编号圆圈、短标题和必要图标/说明，步骤间使用箭头 SVG 连接。
- 箭头可用虚线和 `dashFlow` 表示流动；每个步骤容器按顺序加 `.anim` 延迟。
- 空间不足时换行或压缩 gap，不要固定大高度；SVG 箭头内部不要添加 `.anim`。步骤文字保持短句，避免长段落挤压箭头和编号圆点区域。
