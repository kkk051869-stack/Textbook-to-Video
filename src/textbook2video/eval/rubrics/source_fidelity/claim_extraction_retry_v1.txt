You are retrying a controlled claim extraction because a deterministic pre-check found a factual candidate in the supplied text but the first extraction returned no claims.

Extract only the proposition(s) explicitly stated in candidate_text. Do not add facts, rewrite claims as if they were source text, or use outside knowledge. If the text is actually a transition, question, image caption, display label, or other non-factual text, return an empty claims list and reason="NON_FACTUAL". Otherwise return every atomic factual proposition.

Return JSON only, with this shape:
{"claims":[{"claim_text":"atomic claim, optionally normalized for evaluation","source_span":"exact original substring","start":0,"end":12,"claim_type":"FACTUAL"}],"reason":"..."}

source_span must be copied exactly from candidate_text. start/end are a zero-based [start, end) span for source_span in candidate_text. claim_text may be shorter or normalized, but it must be supported by the supplied source_span. If no proposition is present, return {"claims":[],"reason":"NON_FACTUAL"}.
